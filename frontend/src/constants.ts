export const TEAM_COLORS = ['#3B82F6', '#EF4444', '#10B981', '#F59E0B', '#8B5CF6', '#EC4899'];
export const TEAM_NAMES = ['Blue Team', 'Red Team', 'Green Team', 'Gold Team'];

/** Mobile breakpoint in pixels (matches Tailwind's md). */
export const MOBILE_BREAKPOINT_PX = 768;

/** Width of the buffered corridor used when importing a route, in kilometers. */
export const CORRIDOR_BUFFER_KM = 0.05;

/** Maximum WebSocket reconnect backoff, in milliseconds. */
export const RECONNECT_MAX_DELAY_MS = 30_000;

/** Initial WebSocket reconnect backoff, in milliseconds. */
export const RECONNECT_INITIAL_DELAY_MS = 1_000;
